const numeros = [10, 20, 30];
const produto = {
    nome:'Caneta', 
    preco: 9.99,
    desconto: 0.2
};


console.log(numeros[0]); //mostra no console a posicao do elemento array
console.log(produto['nome']); //mostra no console o nome do elemento
console.log(produto.nome); //mostra no console o nome do elemento
console.log(produto.preco);
console.log(produto.desconto);